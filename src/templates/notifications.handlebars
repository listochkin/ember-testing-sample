<h3 data-test="unread">{{unread}}</h3>

<ul>
	{{#each notification in this}}
	<li>
		<p data-test="title">{{notification.title}}</p>
		<p data-test="isRead">{{notification.isRead}}</p>
		<button data-test="delete" {{action "remove" notification}}>delete</button>
	</li>
	{{/each}}
</ul>
